# List files or directories (relative to the repo root) that should be embedded
# into README.md. Lines starting with # are ignored. Directories pull in every
# .c/.h file they contain, recursively.

component/littlefs-2.11.2/lfs_port.c
component/littlefs-2.11.2/lfs_port.h
Core/Src/main.c

# Example: Core/Src
# Example: Drivers/STM32F4xx_HAL_Driver/Src
