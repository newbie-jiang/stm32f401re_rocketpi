## 功能：串口控制三个LED

**串口**：115200 8N1

控制命令设计，**每条命令以 `\r\n` 结尾**

```
LEDR:ON        LEDR:OFF
LEDG:ON        LEDG:OFF
LEDB:ON        LEDB:OFF
LEDALL:ON      LEDALL:OFF
```



## 1.打开STM32cubeMX，点击ACCESS TO MCU SELECTOR

![image-20251106011342611](https://cloud.rocketpi.club/cloud/image-20251106011342611.png)

## 2.选择芯片型号

- 步骤1：在Commercial Part Number 输入  F401RE
- 步骤2：选择STM32F401RET6 （其他芯片可自行选择）
- 步骤3：点击Start Project

![image-20251106011635971](https://cloud.rocketpi.club/cloud/image-20251106011635971.png)

## 3.晶体振荡器选择

- 步骤1：点击System Core
- 步骤2：点击RCC
- 步骤3：High Speed Clock(HSE) 选择 Crystal/Ceramic Resonator 

![image-20251106012005990](https://cloud.rocketpi.club/cloud/image-20251106012005990.png)

## 4.时钟配置

- 步骤1：点击Clock Configuration  
- 步骤2：input frequency输入8  (因为 rocketpi 硬件设计使用外部晶振为8Mhz，所以此处输入8)
- 步骤3：选择HSE
- 步骤4：在HCLK的输入框中输入84，接着回车，回车之后可能会弹出一个确认框，点击OK,会自动配置时钟，等待配置完毕

![image-20251106013613146](https://cloud.rocketpi.club/cloud/image-20251106013613146.png)

## 5.配置调试接口

- 步骤1：点击SYS
- 步骤2：Debug选择栏  选择Serial Wire

![image-20251106014323818](https://cloud.rocketpi.club/cloud/image-20251106014323818.png)

## 6.配置LED的io为输出

在配置之前看原理图，我们需要看led的原理图，找到LED的原理图部分，从原理可以获得的信息

- 三个LED连接的引脚分别为 PA1,PB10,PB14
- 三个LED都是低电平点亮

![image-20251106014804410](https://cloud.rocketpi.club/cloud/image-20251106014804410.png)

图形化配置

- 步骤1：搜索框中输入PA1，回车，这时候PA1的引脚会闪烁
- 步骤2：点击闪烁的PA1
- 步骤3：将PA1设置为GPIO_Output

![image-20251106015144572](https://cloud.rocketpi.club/cloud/image-20251106015144572.png)

接下来另外两个led的io   PB10,PB14  操作也和上述一样

- 配置完成后确保这三处地地方一致

![image-20251106015611887](https://cloud.rocketpi.club/cloud/image-20251106015611887.png)

点击GPIO,可以看到我们的配置的三个LED出现在这里了

![image-20251106020244581](https://cloud.rocketpi.club/cloud/image-20251106020244581.png)

## 7.配置LE的电气属性

由于三个LED都是低电平点亮，一般 初始化的过程，我们会配置关闭LED，

- 分别点击 PA1,PB10,PB14，将GPIO output level 属性设置为high,配置后烧录程序默认会关闭LED
- 默认的GPIO mode 是推挽输出（推挽输出可以输出高电平，也可以输出低电平），此处我们不需要更改gpio的模式了,至此lLED的配置已经完成了

![image-20251106020625380](https://cloud.rocketpi.club/cloud/image-20251106020625380.png)

## 8.配置UART

PA2是usart调试的发送脚，PA3是usart调试的接收脚

- 点击PA2 设置为USART2_TX
- 点击PA3设置为USART_RX

![image-20251113000631226](https://cloud.rocketpi.club/cloud/image-20251113000631226.png)

开启usart的异步模式

步骤1：点击USART2

步骤2：将USART2的模式配置为 异步（Asynchronous）

![image-20251113001143406](https://cloud.rocketpi.club/cloud/image-20251113001143406.png)

## 9.生成代码

- 步骤1：点击Project Manager
- 步骤2：填写此次要创建的工程名字，（注意这里一定不要使用中文名）
- 步骤3：这个工程保存的路径（注意这里一定不要有中文路径，否则生成的工程会有问题）
- 步骤4：生成的工程选择 MDK-ARM  版本为v5.27（版本可以根据自己安装keil版本选择，一般来说高版本的keil可以向下兼容）

![image-20251106021327300](https://cloud.rocketpi.club/cloud/image-20251106021327300.png)

- 点击Code Generator
- 软件包选择 copy only the necessary files (仅复制使用到的库文件)
- 生成的文件选择  Generate peripheral initialization as a pair .c/.h files perpheral

![image-20251106022353098](https://cloud.rocketpi.club/cloud/image-20251106022353098.png)

最后，点击GENERATE CODE 即可生成代码

![image-20251106022513985](https://cloud.rocketpi.club/cloud/image-20251106022513985.png)

## 10.打开keil工程

- 点击OPen Project即可打开刚创建好的工程（电脑已经安装了keil的前提下）

![image-20251106022629403](https://cloud.rocketpi.club/cloud/image-20251106022629403.png)

## 11.配置串口输入输出

- 在usart.c文件中添加如下代码

添加头文件 

```
 #include <stdio.h> 
```

包含以下代码

```c
#ifdef __GNUC__  // 使用 GCC/ARM-GCC（如 arm-none-eabi-gcc）时的重定向
// 说明：newlib 里 printf/scanf 最终会调用 _write/_read，
// 我们在这里把它们重定向到 UART2，达成 printf→串口、scanf←串口 的效果。

int _write(int file, char *ptr, int len) {
    // 将 ptr 指向的 len 字节数据经 UART2 发送
    // HAL_MAX_DELAY：阻塞等待直到发送完成（注意：在中断上下文中不安全）
    HAL_UART_Transmit(&huart2, (uint8_t*)ptr, len, HAL_MAX_DELAY);
    return len;  // 返回已写入字节数，printf 认为写入成功
}

int _read(int file, char *ptr, int len) {
    // 从 UART2 接收 len 字节到 ptr
    // 同样是阻塞式，直到收到足够字节才返回
    HAL_UART_Receive(&huart2, (uint8_t*)ptr, len, HAL_MAX_DELAY);
    return len;  // 返回已读取字节数，scanf 认为读取成功
}

#elif defined(__ARMCC_VERSION)  // 使用 Keil Arm Compiler（ArmCC/ArmClang）

// 说明：在 ArmCC/ArmClang 下，标准库的 putchar/getchar 默认走 fputc/fgetc。
// 重写它们即可将 printf/scanf 重定向到 UART2。

int fputc(int ch, FILE *f) {
    // 发送单个字符 ch 到 UART2
    uint8_t c = (uint8_t)ch;
    HAL_UART_Transmit(&huart2, &c, 1, HAL_MAX_DELAY);
    return ch;  // 返回发送的字符，表示成功
}

int fgetc(FILE *f) {
    // 从 UART2 阻塞读取一个字节
    uint8_t ch;
    HAL_UART_Receive(&huart2, &ch, 1, HAL_MAX_DELAY);
    return (int)ch;  // 返回读取到的字符
}

#else
    #error "Unsupported compiler"  // 既不是 GCC 也不是 ArmCC，则在编译期报错
#endif

#ifndef __MICROLIB        // 如果没有使用 MicroLIB（Keil 的轻量级 C 库），则执行以下代码

#pragma import(__use_no_semihosting)   // 告诉编译器：不使用半主机模式（Semihosting）

// 定义一个 FILE 结构体，占位用，防止标准库中引用 FILE 出错
struct __FILE
{
    int a;   // 占位成员，没有实际意义
};

// 定义标准输入输出对象（标准库函数如 printf/scanf 会用到）
FILE __stdout;   // 标准输出（如 printf）
FILE __stdin;    // 标准输入（如 scanf）

// 定义系统退出函数（防止链接器报 _sys_exit 未定义的错误）
// 半主机模式中该函数用于程序结束时与调试器通信
// 在裸机环境下无需此功能，所以留空即可
void _sys_exit(int x)
{
    // 空实现，避免半主机调用导致程序卡死
}

#endif   // 结束 #ifndef __MICROLIB 判断

```

mian.c中添加以下代码

头文件

```c
#include <stdio.h>
#include <string.h>
```

代码

```c
#define CMD_BUFFER_LEN 32U

typedef struct
{
  GPIO_TypeDef *port;
  uint16_t pin;
} LedChannel;

static const LedChannel kLedRed = {GPIOA, GPIO_PIN_1};
static const LedChannel kLedGreen = {GPIOB, GPIO_PIN_10};
static const LedChannel kLedBlue = {GPIOB, GPIO_PIN_14};

static uint8_t uart_rx_buffer[CMD_BUFFER_LEN];
static uint8_t uart_rx_index = 0;
/* USER CODE END PV */

/* Private function prototypes -----------------------------------------------*/
void SystemClock_Config(void);
/* USER CODE BEGIN PFP */
static void led_write(const LedChannel *led, GPIO_PinState state);
static void led_on(const LedChannel *led);
static void led_off(const LedChannel *led);
static void led_all(GPIO_PinState state);
static void process_serial_command(const char *cmd);
static void handle_led_command(const LedChannel *led, const char *label, const char *cmd);

/* USER CODE END PFP */

/* Private user code ---------------------------------------------------------*/
/* USER CODE BEGIN 0 */
#define LED_ON_STATE GPIO_PIN_RESET   /* LEDs are wired as active-low */
#define LED_OFF_STATE GPIO_PIN_SET

static void led_write(const LedChannel *led, GPIO_PinState state)
{
  HAL_GPIO_WritePin(led->port, led->pin, state);
}

static void led_on(const LedChannel *led)
{
  led_write(led, LED_ON_STATE);
}

static void led_off(const LedChannel *led)
{
  led_write(led, LED_OFF_STATE);
}

static void led_all(GPIO_PinState state)
{
  led_write(&kLedRed, state);
  led_write(&kLedGreen, state);
  led_write(&kLedBlue, state);
}

static void handle_led_command(const LedChannel *led, const char *label, const char *cmd)
{
  if (strcmp(cmd, "ON") == 0)
  {
    led_on(led);
    printf("%s:ON\r\n", label);
  }
  else if (strcmp(cmd, "OFF") == 0)
  {
    led_off(led);
    printf("%s:OFF\r\n", label);
  }
  else
  {
    printf("%s:UNKNOWN\r\n", label);
  }
}

static void process_serial_command(const char *cmd)
{
  if (strncmp(cmd, "LEDR:", 5) == 0)
  {
    handle_led_command(&kLedRed, "LEDR", cmd + 5);
  }
  else if (strncmp(cmd, "LEDG:", 5) == 0)
  {
    handle_led_command(&kLedGreen, "LEDG", cmd + 5);
  }
  else if (strncmp(cmd, "LEDB:", 5) == 0)
  {
    handle_led_command(&kLedBlue, "LEDB", cmd + 5);
  }
  else if (strncmp(cmd, "LEDALL:", 7) == 0)
  {
    if (strcmp(cmd + 7, "ON") == 0)
    {
      led_all(LED_ON_STATE);
      printf("LEDALL:ON\r\n");
    }
    else if (strcmp(cmd + 7, "OFF") == 0)
    {
      led_all(LED_OFF_STATE);
      printf("LEDALL:OFF\r\n");
    }
    else
    {
      printf("LEDALL:UNKNOWN\r\n");
    }
  }
  else
  {
    printf("UNKNOWN CMD:%s\r\n", cmd);
  }
}
```

while循环中调用

```c
uint8_t byte = 0;
    if (HAL_UART_Receive(&huart2, &byte, 1, HAL_MAX_DELAY) == HAL_OK)
    {
      if (byte == '\r')
      {
        continue;
      }

      if (byte == '\n')
      {
        if (uart_rx_index > 0)
        {
          uart_rx_buffer[uart_rx_index] = '\0';
          process_serial_command((char *)uart_rx_buffer);
          uart_rx_index = 0;
        }
        continue;
      }

      if (uart_rx_index < (CMD_BUFFER_LEN - 1))
      {
        uart_rx_buffer[uart_rx_index++] = byte;
      }
      else
      {
        /* Command too long, reset to avoid stale data */
        uart_rx_index = 0;
      }
    }
```



整体main.c代码

```c
/* USER CODE BEGIN Header */
/**
  ******************************************************************************
  * @file           : main.c
  * @brief          : Main program body
  ******************************************************************************
  * @attention
  *
  * Copyright (c) 2025 STMicroelectronics.
  * All rights reserved.
  *
  * This software is licensed under terms that can be found in the LICENSE file
  * in the root directory of this software component.
  * If no LICENSE file comes with this software, it is provided AS-IS.
  *
  ******************************************************************************
  */
/* USER CODE END Header */
/* Includes ------------------------------------------------------------------*/
#include "main.h"
#include "usart.h"
#include "gpio.h"

/* Private includes ----------------------------------------------------------*/
/* USER CODE BEGIN Includes */
#include <stdio.h>
#include <string.h>
/* USER CODE END Includes */

/* Private typedef -----------------------------------------------------------*/
/* USER CODE BEGIN PTD */

/* USER CODE END PTD */

/* Private define ------------------------------------------------------------*/
/* USER CODE BEGIN PD */

/* USER CODE END PD */

/* Private macro -------------------------------------------------------------*/
/* USER CODE BEGIN PM */

/* USER CODE END PM */

/* Private variables ---------------------------------------------------------*/

/* USER CODE BEGIN PV */
#define CMD_BUFFER_LEN 32U

typedef struct
{
  GPIO_TypeDef *port;
  uint16_t pin;
} LedChannel;

static const LedChannel kLedRed = {GPIOA, GPIO_PIN_1};
static const LedChannel kLedGreen = {GPIOB, GPIO_PIN_10};
static const LedChannel kLedBlue = {GPIOB, GPIO_PIN_14};

static uint8_t uart_rx_buffer[CMD_BUFFER_LEN];
static uint8_t uart_rx_index = 0;
/* USER CODE END PV */

/* Private function prototypes -----------------------------------------------*/
void SystemClock_Config(void);
/* USER CODE BEGIN PFP */
static void led_write(const LedChannel *led, GPIO_PinState state);
static void led_on(const LedChannel *led);
static void led_off(const LedChannel *led);
static void led_all(GPIO_PinState state);
static void process_serial_command(const char *cmd);
static void handle_led_command(const LedChannel *led, const char *label, const char *cmd);

/* USER CODE END PFP */

/* Private user code ---------------------------------------------------------*/
/* USER CODE BEGIN 0 */
#define LED_ON_STATE GPIO_PIN_RESET   /* LEDs are wired as active-low */
#define LED_OFF_STATE GPIO_PIN_SET

static void led_write(const LedChannel *led, GPIO_PinState state)
{
  HAL_GPIO_WritePin(led->port, led->pin, state);
}

static void led_on(const LedChannel *led)
{
  led_write(led, LED_ON_STATE);
}

static void led_off(const LedChannel *led)
{
  led_write(led, LED_OFF_STATE);
}

static void led_all(GPIO_PinState state)
{
  led_write(&kLedRed, state);
  led_write(&kLedGreen, state);
  led_write(&kLedBlue, state);
}

static void handle_led_command(const LedChannel *led, const char *label, const char *cmd)
{
  if (strcmp(cmd, "ON") == 0)
  {
    led_on(led);
    printf("%s:ON\r\n", label);
  }
  else if (strcmp(cmd, "OFF") == 0)
  {
    led_off(led);
    printf("%s:OFF\r\n", label);
  }
  else
  {
    printf("%s:UNKNOWN\r\n", label);
  }
}

static void process_serial_command(const char *cmd)
{
  if (strncmp(cmd, "LEDR:", 5) == 0)
  {
    handle_led_command(&kLedRed, "LEDR", cmd + 5);
  }
  else if (strncmp(cmd, "LEDG:", 5) == 0)
  {
    handle_led_command(&kLedGreen, "LEDG", cmd + 5);
  }
  else if (strncmp(cmd, "LEDB:", 5) == 0)
  {
    handle_led_command(&kLedBlue, "LEDB", cmd + 5);
  }
  else if (strncmp(cmd, "LEDALL:", 7) == 0)
  {
    if (strcmp(cmd + 7, "ON") == 0)
    {
      led_all(LED_ON_STATE);
      printf("LEDALL:ON\r\n");
    }
    else if (strcmp(cmd + 7, "OFF") == 0)
    {
      led_all(LED_OFF_STATE);
      printf("LEDALL:OFF\r\n");
    }
    else
    {
      printf("LEDALL:UNKNOWN\r\n");
    }
  }
  else
  {
    printf("UNKNOWN CMD:%s\r\n", cmd);
  }
}

/* USER CODE END 0 */

/**
  * @brief  The application entry point.
  * @retval int
  */
int main(void)
{

  /* USER CODE BEGIN 1 */

  /* USER CODE END 1 */

  /* MCU Configuration--------------------------------------------------------*/

  /* Reset of all peripherals, Initializes the Flash interface and the Systick. */
  HAL_Init();

  /* USER CODE BEGIN Init */

  /* USER CODE END Init */

  /* Configure the system clock */
  SystemClock_Config();

  /* USER CODE BEGIN SysInit */

  /* USER CODE END SysInit */

  /* Initialize all configured peripherals */
  MX_GPIO_Init();
  MX_USART2_UART_Init();
  /* USER CODE BEGIN 2 */

  /* USER CODE END 2 */

  /* Infinite loop */
  /* USER CODE BEGIN WHILE */
  while (1)
  {
    uint8_t byte = 0;
    if (HAL_UART_Receive(&huart2, &byte, 1, HAL_MAX_DELAY) == HAL_OK)
    {
      if (byte == '\r')
      {
        continue;
      }

      if (byte == '\n')
      {
        if (uart_rx_index > 0)
        {
          uart_rx_buffer[uart_rx_index] = '\0';
          process_serial_command((char *)uart_rx_buffer);
          uart_rx_index = 0;
        }
        continue;
      }

      if (uart_rx_index < (CMD_BUFFER_LEN - 1))
      {
        uart_rx_buffer[uart_rx_index++] = byte;
      }
      else
      {
        /* Command too long, reset to avoid stale data */
        uart_rx_index = 0;
      }
    }
    /* USER CODE END WHILE */

    /* USER CODE BEGIN 3 */
  }
  /* USER CODE END 3 */
}

/**
  * @brief System Clock Configuration
  * @retval None
  */
void SystemClock_Config(void)
{
  RCC_OscInitTypeDef RCC_OscInitStruct = {0};
  RCC_ClkInitTypeDef RCC_ClkInitStruct = {0};

  /** Configure the main internal regulator output voltage
  */
  __HAL_RCC_PWR_CLK_ENABLE();
  __HAL_PWR_VOLTAGESCALING_CONFIG(PWR_REGULATOR_VOLTAGE_SCALE2);

  /** Initializes the RCC Oscillators according to the specified parameters
  * in the RCC_OscInitTypeDef structure.
  */
  RCC_OscInitStruct.OscillatorType = RCC_OSCILLATORTYPE_HSE;
  RCC_OscInitStruct.HSEState = RCC_HSE_ON;
  RCC_OscInitStruct.PLL.PLLState = RCC_PLL_ON;
  RCC_OscInitStruct.PLL.PLLSource = RCC_PLLSOURCE_HSE;
  RCC_OscInitStruct.PLL.PLLM = 4;
  RCC_OscInitStruct.PLL.PLLN = 84;
  RCC_OscInitStruct.PLL.PLLP = RCC_PLLP_DIV2;
  RCC_OscInitStruct.PLL.PLLQ = 4;
  if (HAL_RCC_OscConfig(&RCC_OscInitStruct) != HAL_OK)
  {
    Error_Handler();
  }

  /** Initializes the CPU, AHB and APB buses clocks
  */
  RCC_ClkInitStruct.ClockType = RCC_CLOCKTYPE_HCLK|RCC_CLOCKTYPE_SYSCLK
                              |RCC_CLOCKTYPE_PCLK1|RCC_CLOCKTYPE_PCLK2;
  RCC_ClkInitStruct.SYSCLKSource = RCC_SYSCLKSOURCE_PLLCLK;
  RCC_ClkInitStruct.AHBCLKDivider = RCC_SYSCLK_DIV1;
  RCC_ClkInitStruct.APB1CLKDivider = RCC_HCLK_DIV2;
  RCC_ClkInitStruct.APB2CLKDivider = RCC_HCLK_DIV1;

  if (HAL_RCC_ClockConfig(&RCC_ClkInitStruct, FLASH_LATENCY_2) != HAL_OK)
  {
    Error_Handler();
  }
}

/* USER CODE BEGIN 4 */

/* USER CODE END 4 */

/**
  * @brief  This function is executed in case of error occurrence.
  * @retval None
  */
void Error_Handler(void)
{
  /* USER CODE BEGIN Error_Handler_Debug */
  /* User can add his own implementation to report the HAL error return state */
  __disable_irq();
  while (1)
  {
  }
  /* USER CODE END Error_Handler_Debug */
}

#ifdef  USE_FULL_ASSERT
/**
  * @brief  Reports the name of the source file and the source line number
  *         where the assert_param error has occurred.
  * @param  file: pointer to the source file name
  * @param  line: assert_param error line source number
  * @retval None
  */
void assert_failed(uint8_t *file, uint32_t line)
{
  /* USER CODE BEGIN 6 */
  /* User can add his own implementation to report the file name and line number,
     ex: printf("Wrong parameters value: file %s on line %d\r\n", file, line) */
  /* USER CODE END 6 */
}
#endif /* USE_FULL_ASSERT */

```



