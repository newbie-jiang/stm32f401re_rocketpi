## UART协议解析

UART（Universal Asynchronous Receiver/Transmitter，通用异步收发器）与 USART（Universal Synchronous/Asynchronous Receiver/Transmitter，通用同步/异步收发器）在名称上只差一个字母“S”，但功能上有明显区别。下面是详细对比：

### 🧩 一、基本概念区别

| 项目     | UART                                        | USART                                                   |
| -------- | ------------------------------------------- | ------------------------------------------------------- |
| 全称     | Universal Asynchronous Receiver/Transmitter | Universal Synchronous/Asynchronous Receiver/Transmitter |
| 翻译     | 通用异步收发器                              | 通用同步/异步收发器                                     |
| 通信方式 | **异步通信**（Asynchronous）                | **既可同步，也可异步通信**                              |
| 时钟信号 | **无独立时钟线**，通过波特率同步            | **有独立时钟线（同步模式）**，可与外设共用时钟          |

------

### ⚙️ 二、工作方式对比

#### 1️⃣ UART 工作方式（异步）

- 发送端和接收端**各自有时钟**，通过约定好的**波特率**保持同步；
- 帧格式：起始位 + 数据位 + 校验位（可选）+ 停止位；
- 没有时钟线，只用 TX 和 RX 两根线。

📘 **示意：**

```
空闲(高) ──┐┌──────┐┌────┐┌──────┐┌──────┐┌────┐
           ↓   ↓      ↓    ↓      ↓      ↓
         起始位 数据位0 数据位1 ... 校验位 停止位
```

------

#### 2️⃣ USART 工作方式（同步 + 异步）

- 同步模式下，主设备提供**时钟信号线 (CLK)**，数据在时钟沿上发送和采样；
- 异步模式下，与 UART 完全相同；
- 通常多出一根 `SCLK` 引脚；
- 常用于对时要求高的通信，如高速模块、外设控制等。

📘 **同步模式示意：**

```
主机发送：CLK ↑   ↑   ↑   ↑
           TX ──▁▁▀▀▁▁▀▀▁▁▀▀▁▁▀▀▁▁──
           RX ──▀▀▁▁▀▀▁▁▀▀▁▁▀▀▁▁▀▀▁▁──
```

------

### 🔍 三、硬件上区别

- **UART 模块**：只支持异步通信；
- **USART 模块**：在硬件上包含 UART 功能，还额外支持同步通信；
- 例如 STM32 系列中，标为 “USARTx” 的外设既能当 UART 用，也能当同步串口用。

------

### 📡 四、典型使用场景

| 类型          | 常见用途                                             |
| ------------- | ---------------------------------------------------- |
| UART          | 串口调试（TTL 串口、蓝牙模块、GPS 模块等）           |
| USART（同步） | SPI 替代、与外部通信芯片同步通信、要求高速可靠的场合 |
| USART（异步） | 与 UART 完全相同，可互通                             |

------

### 💡 五、总结一句话

> **USART = UART + 同步通信功能**

如果你只用异步方式（比如普通串口通信），UART 和 USART 是一样的；
 但如果要精确同步数据传输（比如用外部时钟线），那就需要用 USART 的同步模式。



**我们最常用的是UART或者USART的异步模式  1bit的起始位+8bit的数据位+1bit的停止位**

实验使用USB转TTL  常见的有ch340 cp2102     使用逻辑分析仪捕获UART TX脚的数据

## 示例1：接收十六进制数据

- 波特率115200
- 起始位1位
- 数据位8位
- 停止位1位
- 发送十六进制数据0xABC 一共是12位

![image-20251112230209485](https://cloud.rocketpi.club/cloud/image-20251112230209485.png)

下面使用逻辑分析仪对uart信号进行捕获

- 连接好硬件
- 配置好逻辑分析仪上位机参数

![image-20251112225433889](https://cloud.rocketpi.club/cloud/image-20251112225433889.png)

- 点击捕获

![image-20251112225523896](https://cloud.rocketpi.club/cloud/image-20251112225523896.png)

![image-20251112225919780](https://cloud.rocketpi.club/cloud/image-20251112225919780.png)

以上是我们捕获到的信号，来对信号进行解析

uart协议由 起始位+数据位+停止位组成 这里我们并没有添加基偶校验，起始位1位+数据位8位+停止位1位

0XAB协议解析为 0110101011 我们去掉起始位和停止位，数据位为 1101 0101

又由于UART的数据位为LSB 所以我们实际得到的数据为： 1010 1011 即对应0xAB

再来看一下0x0C 协议解析为 00011 00001 掐头去尾之得到的数据为 0011 0000，LSB转化后就是0x0C

补充

 **LSB**   （Least Significant Bit）  最低有效位（数据的最右边一位，权值最小）

## 示例2：字符数据

![image-20251112230135807](https://cloud.rocketpi.club/cloud/image-20251112230135807.png)

![image-20251112230116228](https://cloud.rocketpi.club/cloud/image-20251112230116228.png)

这里ABC对应的数据为0x41= 65 0x42=66 0x43 = 67 实际上就是ASCII

[![image-20231202200947835](https://cloud.rocketpi.club/cloud/image-20231202200947835.png)](https://newbie-typora.oss-cn-shenzhen.aliyuncs.com/TyporaJPG/image-20231202200947835.png)

## 示例3：添加换行

![image-20251112230524373](https://cloud.rocketpi.club/cloud/image-20251112230524373.png)

![image-20251112230349808](https://cloud.rocketpi.club/cloud/image-20251112230349808.png)

[![image-20231202202905818](https://cloud.rocketpi.club/cloud/image-20231202202905818.png)](https://newbie-typora.oss-cn-shenzhen.aliyuncs.com/TyporaJPG/image-20231202202905818.png)

解释如下：

根据提供的逻辑分析仪截图，显示了一系列UART数据帧，包括十六进制数值 `0x41`、`0x42`、`0x43`、`0x0D` 和 `0x0A`。

- `0x41` 对应于ASCII字符 'A'
- `0x42` 对应于ASCII字符 'B'
- `0x43` 对应于ASCII字符 'C'

接下来的两个值 `0x0D` 和 `0x0A` 对应于回车（Carriage Return, CR）和换行（Line Feed, LF）控制字符：

- `0x0D` 是回车（CR），在ASCII中用于指示光标位置回到一行的开头。
- `0x0A` 是换行（LF），在ASCII中用于指示光标移动到下一行。

在文本通信中，特别是基于Windows系统的环境，字符序列 CR 和 LF 一起使用 (`\r\n`) 来表示新的一行的开始。在Unix/Linux系统中，通常只使用换行（LF，`\n`）来表示新行，而在Mac系统的早期版本中，只使用回车（CR，`\r`）。

因此，`0x0D` 和 `0x0A` 表明您发送了 'ABC' 后跟一个新行命令。这符合您描述的发送 'ABC' 并换行的行为。



## 回头再看看波特率的计算

**波特率（Baud Rate）** 表示 **每秒传输的符号数（比特数）**。

我们使用的波特率为115200，实际对应1秒发送115200个bit， 

发送1个bit的数据，理论时间为 1s/115200 = 8.68us

![image-20251112231911097](https://cloud.rocketpi.club/cloud/image-20251112231911097.png)

再看看逻辑分析仪的一个bit时间 （8.67us）  时间上几乎一致

![image-20251112232045224](https://cloud.rocketpi.club/cloud/image-20251112232045224.png)

再看看波特率设置为9600一个bit的时间

理论值计算：1/9600 = 104.1us

![image-20251112233154120](https://cloud.rocketpi.club/cloud/image-20251112233154120.png)

![image-20251112232953331](https://cloud.rocketpi.club/cloud/image-20251112232953331.png)

发送任意字符

![image-20251112233107751](https://cloud.rocketpi.club/cloud/image-20251112233107751.png)

**波特率9600实际测量值 一个bit 103.82 us**   几乎一致

![image-20251112233321304](https://cloud.rocketpi.club/cloud/image-20251112233321304.png)



## 总结：

UART发送的数据位最常见的  **1bit起始位 8bit数据位，1bit 停止位  使用LSB**

UART 波特率常用 **9600、115200**，是因为：

- 它们能被常见晶振整除（误差小）；
- 来源于历史标准，通用兼容性好；
- 被硬件和驱动固化支持。